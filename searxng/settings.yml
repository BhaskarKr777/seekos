use_default_settings:
  engines:
    keep_only:
      - brave
      - duckduckgo

general:
  instance_name: "SeekOS Search"

search:
  formats:
    - html
    - json

server:
  secret_key: "seekos-development-secret-change-me"